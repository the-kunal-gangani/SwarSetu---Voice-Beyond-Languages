import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const deepNavy = Color(0xFF071A33);
  static const midnightBlue = Color(0xFF15294A);
  static const royalBlue = Color(0xFF155EEF);
  static const electricBlue = Color(0xFF2F80FF);
  static const cyan = Color(0xFF35D5FF);
  static const softBlue = Color(0xFF8ED8FF);

  // Voice accent (warm, used sparingly for mic + result moments)
  static const voiceCoral = Color(0xFFFF6B6B);
  static const voiceMagenta = Color(0xFFEC4899);
  static const voiceViolet = Color(0xFF9B5DE5);

  // Background
  static const background = Color(0xFFF5F9FF);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF071A33);
  static const textSecondary = Color(0xFF64748B);

  // Status
  static const success = Color(0xFF16A34A);
  static const error = Color(0xFFDC2626);
  static const warning = Color(0xFFF59E0B);
}
